<Window x:Class="Yes.TypeAddWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Yes"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="TypeAddWin" Height="100" Width="400" Closing="Close">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBox x:Name="type_name" Width="300" materialDesign:HintAssist.Hint="Введите новый тип записи" Grid.Row="0" HorizontalContentAlignment="Center" TextAlignment="Center"/>

        <Button x:Name="add_type"
                Width="300"
                Grid.Row="3" Grid.Column="1" VerticalAlignment="Top" HorizontalAlignment="Center"
                Content="Добавить новый тип записи"
                Style="{StaticResource MaterialDesignFlatDarkButton}"
                ToolTip="MaterialDesignFlatButton" Click="add_type_Click" FontSize="10"/>
    </Grid>
</Window>
